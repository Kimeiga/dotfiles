# nix build out symlink
result*
# packer.nvim generated plugin loader code
packer_compiled.lua
.DS_Store

# Secrets - IMPORTANT: Only commit encrypted SOPS files in secrets/ directory!
# Allow encrypted .env files in secrets/ directory (negation must come BEFORE the wildcard)
!secrets/*.env
# Ignore .env files everywhere else
*.env
.env
.env.*
secrets/zshrc-snippet.txt  # This is just a template, not needed in repo

# SOPS age private keys (NEVER commit these!)
keys.txt
*.key
.age-key*

# Backup files
*.bak
*.backup
*~

# Secret scanning reports
reports/
.git-filter-repo/